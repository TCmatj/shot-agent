# CLAUDE.md

本文件是 `shot-agent` 的项目级指令。Claude Code 在本仓库工作时，应优先阅读并遵守本文档。

## 项目目标

`shot-agent` 目标是提供一个干净、简洁的无限画布，用于视觉创作。

项目会逐步接入主流图片生成模型和视频生成模型，让用户可以在一个开放工作区中创建、摆放、对比和迭代生成素材。

第一期重点接入：

- `gpt-image-2`
- `seedance2.0`
- `seedance2.0-fast`

## 推荐技术栈

本项目优先采用以下技术栈：

- `TypeScript`：默认开发语言，减少模型接入和画布状态管理中的类型歧义。
- `React`：构建交互式画布、侧边栏、任务面板和素材节点。
- `Vite`：作为前端开发和构建工具，保持项目轻量、启动快。
- 成熟无限画布库优先：优先评估 `tldraw`、`reactflow`、`pixi.js` 等成熟方案；不要在第一期直接重写完整无限画布基础能力。
- 模型 API 适配层：把图片模型、视频模型的调用封装在独立模块中，避免 UI 直接依赖具体供应商 API。
- 环境变量：API key、模型 endpoint、鉴权信息只能通过环境变量或本地未提交配置读取。
- 对象存储：第一期本地存储和 Cloudflare R2 优先，其他 S3 兼容存储后续扩展。

除非用户明确要求，否则不要引入重型全栈框架、桌面端壳、复杂后端或数据库。第一期优先验证画布体验和模型生成流程。

## 目录规划

建议后续代码结构按以下方向演进：

```text
src/
  app/            # 应用入口、路由、全局状态装配
  canvas/         # 无限画布、节点、选择、拖拽、缩放、布局
  models/         # 图片模型和视频模型适配层
  assets/         # 静态资源
  components/     # 通用 UI 组件
  hooks/          # React hooks
  lib/            # 通用工具、类型、错误处理
  styles/         # 全局样式和设计变量
docs/
  external-api/   # 外部图片、视频模型 API 调研与适配说明
test/
  unit/           # 单元测试
  integration/    # 模型适配层、任务流、状态流转测试
  e2e/            # 关键用户路径和画布交互测试
  fixtures/       # 测试夹具、mock 响应、示例素材
```

实际落地时应服从已有项目结构，不为目录规划做无意义搬迁。

## 媒体节点与模型兼容

项目应从第一期开始考虑图片节点和视频节点的兼容性，但不要假设图片 API 与视频 API 存在完全统一的行业标准。

调研结论：

- OpenAI `gpt-image-2` 面向图片生成和编辑，输入支持文本、图片，输出为图片，可通过 Image API 或 Responses API 使用。
- Seedance 2.0 / 2.0 Fast 面向视频生成，常见接口形态是异步任务：提交任务、轮询或回调获取状态、最终得到视频 URL。
- Seedance 2.0 的视频任务通常支持文本、图片、视频、音频等多模态输入，并存在首帧、末帧、参考图、参考视频、参考音频等角色差异。
- 图片生成更偏同步或短任务，视频生成更偏长耗时异步任务；二者不能直接用同一层 API 参数硬套。

因此项目内部应定义稳定的统一抽象，再由模型适配器映射到具体供应商 API：

```ts
type MediaKind = 'image' | 'video' | 'audio';

type CanvasNodeKind = 'image' | 'video';

type GenerationInputRole =
  | 'prompt'
  | 'source_image'
  | 'first_frame'
  | 'last_frame'
  | 'reference_image'
  | 'reference_video'
  | 'reference_audio';

type GenerationStatus =
  | 'idle'
  | 'queued'
  | 'running'
  | 'succeeded'
  | 'failed'
  | 'canceled';
```

实现要求：

- 画布节点关注展示和编辑：位置、尺寸、缩放、选中、预览、元数据。
- 生成任务关注模型调用：输入、模型、参数、状态、进度、错误、输出素材。
- 图片节点和视频节点都应挂载到统一的 `asset` / `generation` 元数据上，但保留各自字段。
- 图片节点至少记录宽高、格式、透明背景、来源任务。
- 视频节点至少记录宽高、比例、时长、帧率、封面、是否含音频、来源任务。
- 不要让 UI 组件直接拼接 OpenAI 或 Seedance 请求体；必须通过 `models/` 下的适配器转换。
- 适配器输出统一的任务结果结构，内部保留原始响应用于调试。
- 测试中必须覆盖图片生成结果、视频异步任务状态流转、失败重试和取消。

## 开发原则

- 以画布体验为中心，保持界面干净、直接、低干扰。
- 优先做可工作的最小闭环：输入提示词、提交生成任务、展示结果、在画布中继续迭代。
- 模型接入必须有清晰边界：UI、任务状态、模型 API 适配层分离。
- 不要把供应商 API 细节散落在组件里。
- 对长耗时任务提供加载、失败、重试和取消状态。
- 任何密钥、token、cookie、私有 endpoint 都不得提交到仓库。
- 能使用中文的项目文档、注释、规范和说明尽量使用中文；英文 README 除外。
- 所有用户可见中文文案必须避免乱码：源码文件统一按 UTF-8 处理，不要把终端、日志或错误输出里已经乱码的文本直接复制回源码。
- 修改、新增中文 UI 文案后，应主动检查按钮、标题、提示、模态框等实际显示结果；如果发现乱码，优先从源文本和编码处理上修正，而不是继续复用乱码字符串。

## 浏览器与桌面兼容

本项目已进入浏览器与桌面双运行时阶段，后续开发默认同时考虑两种运行方式：

- 浏览器模式：`Vite + 浏览器 File System Access API`
- 桌面模式：`Tauri + WebView + 本地文件系统`

开发要求：

- 不能只以“浏览器可用”为完成标准；若变更会影响桌面端交互、文件系统、快捷键、窗口行为或构建流程，必须一并考虑。
- 涉及目录选择、文件读写、资产落盘、工作区恢复、导入导出时，禁止在业务组件中直接绑定浏览器专属 API。
- 文件系统相关能力必须统一走 `src/storage/` 下的抽象层，尤其是：
  - `src/storage/index.ts`
  - `src/storage/workspaceStore.ts`
  - `src/storage/browserWorkspaceStore.ts`
  - `src/storage/desktopWorkspaceStore.ts`
- 新增存储能力时，应先判断是否属于浏览器/桌面共性；共性逻辑优先抽到统一接口，再分别适配。
- 涉及 UI 交互时，应注意浏览器和桌面 WebView 在以下方面可能存在差异：
  - 文件/目录选择器触发方式
  - `Command` / `Ctrl` 类快捷键
  - 拖拽、滚轮、触控板缩放
  - 系统级打开文件、路径权限、窗口焦点
- 若某次需求明确只要求浏览器端或只要求桌面端，需在实现说明中写清范围，不默认扩展另一端。
- 提交前至少确认本次改动没有破坏另一运行时的主路径；若无法验证，必须在结果说明中明确写出未验证项。

## README 维护

本项目主 README 使用中文：

- `README.md`：中文主文档
- `README.en.md`：英文文档

每次修改 README 内容时，必须同步更新两个文件，确保信息一致。

## Commit 规范

commit message 使用英文类型前缀和中文说明：

```text
<type>: <中文简短说明>
```

常用类型：

- `feat`：新增功能
- `fix`：修复问题
- `doc`：文档修改
- `refactor`：重构
- `perf`：性能优化
- `test`：测试
- `build`：构建或依赖
- `ci`：CI/CD
- `style`：格式或样式
- `chore`：仓库维护或杂项
- `revert`：回滚

示例：

```text
feat: 接入 gpt-image-2 图片生成入口
fix: 修复画布元素拖拽丢失焦点问题
doc: 拆分中英文 README
```

提交前应检查 `git status --short` 和相关 diff，避免混入无关变更。

## PR 规范

PR 使用中文编写，除非目标仓库明确要求英文。

PR 标题格式：

```text
<type>: <中文简短说明>
```

PR 描述建议包含：

```markdown
## 变更内容

- 

## 变更原因

- 

## 验证方式

- 

## 影响范围

- 

## 风险与回滚

- 
```

创建或更新 PR 前，确认：

- 当前分支只包含相关变更。
- 已运行与变更最相关的验证命令。
- 已检查 diff，确认没有误提交密钥、调试日志、生成物和无关文件。
- README 变更已同步更新中英文版本。
- commit message 符合本项目规范。

## 自动 Review 要求

提交或 PR 前，应优先检查：

- 正确性问题和边界场景
- 画布交互是否存在明显回归
- 加载、空态、错误态、取消态是否完整
- 模型任务失败、超时、重试是否可控
- 密钥和环境变量是否安全
- README 中英文是否同步
- 是否存在无关变更、日志、临时文件或本地配置

Review 输出应先列问题，再补充总结；没有发现问题时，也要说明已验证内容和剩余风险。

## 测试与验证

仓库尚未建立具体测试命令时，不要编造测试结果。每次变更后至少执行与变更匹配的可用检查，例如：

- 文档变更：检查相关 Markdown 文件内容和链接。
- skill 变更：运行 `quick_validate.py` 校验 skill。
- 前端代码变更：优先运行项目提供的 `lint`、`test`、`build` 命令。

如果某项验证无法运行，必须在最终说明中写清原因。

## Claude 工作要求

- 开始修改前先查看当前文件和 git 状态。
- 尊重用户已有改动，不要回滚未确认的变更。
- 修改前说明将要改什么。
- 修改后检查 diff 和状态。
- 对真实文件引用使用绝对路径。
- 变更项目文档时，能使用中文的内容尽量使用中文。
